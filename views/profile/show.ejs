<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>My Profile - RoomLink</title>
  <link rel="stylesheet" href="/styles.css" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
</head>
<body>
  <header class="container header">
    <a href="/" class="brand">RoomLink</a>
    <nav class="nav">
      <a href="/">Home</a>
      <a href="/flats">Manage Your Flats</a>
      <a class="btn" href="/flats/new">Add Flat</a>
      <a href="/chat">Chat</a>
      <% if (currentUser) { %>
        <a href="/profile">My Profile</a>
        <form method="POST" action="/logout" style="display:inline">
          <button class="btn btn-secondary" type="submit">Logout</button>
        </form>
      <% } else { %>
        <a class="btn" href="/register">Get Started</a>
      <% } %>
    </nav>
  </header>

  <main class="container">
    <section class="card profile-detail">
      <img class="detail-photo" src="<%= user.avatarUrl || 'https://placehold.co/200x200?text=Avatar' %>" alt="Avatar" style="width:200px;height:200px;object-fit:cover" />
      <div>
        <h1 class="title"><%= user.name %></h1>
        <div class="meta"><%= user.email %></div>
        <% if (user.phone) { %>
          <div class="meta">Phone: <%= user.phone %></div>
        <% } %>
        <div class="actions" style="margin-top:10px">
          <a class="btn" href="/profile/edit">Edit Profile</a>
        </div>
      </div>
    </section>
  </main>

  <footer class="container footer">
    <div>
      <span class="brand">RoomLink</span> • © <%= new Date().getFullYear() %>
      <div style="margin-top:6px">
        <a href="/" style="color:inherit;text-decoration:none;margin-right:8px">Home</a>
        <a href="/flats" style="color:inherit;text-decoration:none;margin-right:8px">Manage Your Flats</a>
        <% if (!currentUser) { %>
          <a href="/register" style="color:inherit;text-decoration:none">Get Started</a>
        <% } else { %>
          <a href="/profile" style="color:inherit;text-decoration:none">My Profile</a>
        <% } %>
      </div>
    </div>
  </footer>
</body>
</html>
